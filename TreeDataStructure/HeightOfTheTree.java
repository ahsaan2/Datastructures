package TreeDataStructure;

public class HeightOfTheTree {
    // find the height of the binary search tree
   // if we are standing at any node(suppose we are at the root node), our answer will be the 
   // 1 + maximum height of the left side and the maximum height of the right side
   // 1 + max(left, right)
}
