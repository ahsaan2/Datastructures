package ObjectOrientedProgramming.PrincipleOfOops;

public class Inheritance {
    /**
     * Inheritance is a mechanism in which one class is allowed to inherit the
     * properties(methods and fields) of another class.
     * Inheritance means creating new classes based on the previous or existing
     * ones.
     * -->> Child class is inheriting properties from the base class.
     * the extends keyword is used for inheritance in java
     * class child extends base class.
     * Child class will have some of the properties of the base class and it will
     * have some of the properties of its own.
     * Whenever we create a constructor in the child class we have to initialize the
     * parent class variables also.
     * 
     */
}
